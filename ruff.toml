line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I"]
ignore = ["E501"]   # line length is enforced by the formatter

[lint.per-file-ignores]
"dags/*" = ["E402"]   # Airflow tasks often import inside functions